{
  "name": "composio-session-replay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test 'test/**/*.test.ts'",
    "test:e2e": "tsx --test 'test/e2e/**/*.test.ts'",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@composio/client": "2.0.0-rc.7"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
